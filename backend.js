export const API = () => {
  const endpoint = "https://vardhamangruhudhyog.in";
  const url = endpoint;
  return `${url}/api`;
};

export const IMG = () => {
  const endpoint = "https://vardhamangruhudhyog.in";
  const url = endpoint;
  return `${url}`;
};
