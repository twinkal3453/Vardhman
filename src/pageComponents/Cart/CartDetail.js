import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CartDetail = () => {
  return (
    <View style={styles.detail_component}>
      <Text>CartDetail Twinkal</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  detail_component: {
    padding: 2,
  },
});

export default CartDetail;
