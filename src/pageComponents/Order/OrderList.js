import { StyleSheet, Text, View } from "react-native";
import React from "react";

const OrderList = () => {
  return (
    <View style={styles.order_component}>
      <Text>OrderList Twinkal</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  order_component: {
    padding: 2,
  },
});

export default OrderList;
